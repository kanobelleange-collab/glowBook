namespace Domain.Enum;

public enum UserRole
{
    Admin = 1,         // Le superviseur (ton module 4.4)
    Client = 2,        // L'utilisateur standard (ton module 4.1)
    Etablissement = 3, // Le propriétaire (ton module 4.2)
    Employee = 4       // Le praticien (ton module 4.3)
}

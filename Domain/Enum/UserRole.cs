namespace Domain.Enum;

public enum UserRole
{
    Admin,         // Le superviseur (ton module 4.4)
    Client,        // L'utilisateur standard (ton module 4.1)
    Etablissement, // Le propriétaire (ton module 4.2)
    Employee       // Le praticien (ton module 4.3)
}
